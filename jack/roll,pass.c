#include<stdio.h>
#include<conio.h>
void main()
{
    int roll,pass;
    printf("enter the roll=");
    scanf("%d",&roll);
    printf("enter the pass=");
    scanf("%d",&pass);

    if(roll==112)
    {
        printf("ok");
    }
    if(pass==113)
    {
        printf("no");
    }

    getch();
}